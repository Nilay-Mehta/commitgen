# Target environment: Google Colab (free T4). Not all of these install cleanly
# on a CPU-only Windows laptop — bitsandbytes in particular is Linux+CUDA only.
# Local dev only needs: datasets, transformers, pandas (for quick data peeks).

transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.10.1
bitsandbytes>=0.43.0
accelerate>=0.33.0
torch>=2.3.0

# Evaluation (Day 4)
evaluate>=0.4.2
sacrebleu>=2.4.0
rouge-score>=0.1.2

# Utilities
pandas>=2.0.0
tqdm>=4.66.0
